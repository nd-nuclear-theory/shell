Example 01: harmonic oscillator run with interaction tbmes given directly at hw

4He Nmax06 hw20


% python3 h2mixer_input.py

% setenv SHELL_DIR ${HOME}/projects/shell/install

% csh run.csh

% mpiexec -n 1 /home/mcaprio/projects/mfdn/mfdn-v14-beta06-newmake/xmfdn-h2-lan.exe




foreach f (*.bin)
${SHELL_DIR}/bin/h2stat --matrices $f > ${f:r}.mat
end
